# ai
ai projects 
# Requirments:
Python 3.11
pip install opencv-python 
python -m pip install mediapipe==0.10.9
python -c "import mediapipe as mp; print(hasattr(mp, 'solutions')); print(mp.solutions)"
